* Use this MQSC file to apply to all Native-HA queue managers.
DEFINE QLOCAL(IBM.DEMO.Q) BOQNAME(IBM.DEMO.Q.BOQ) BOTHRESH(3) MAXDEPTH(100000) REPLACE
DEFINE QLOCAL(IBM.DEMO.Q.BOQ) REPLACE
* Use a different dead letter queue, for undeliverable messages
DEFINE QLOCAL('DEAD.LETTER.QUEUE') REPLACE
ALTER QMGR DEADQ('DEAD.LETTER.QUEUE')
DEFINE CHANNEL('IBM.APP.SVRCONN') CHLTYPE(SVRCONN) SSLCIPH(ANY) SSLCAUTH(OPTIONAL) SHARECNV(10) HBINT(3) REPLACE
ALTER QMGR CHLAUTH (DISABLED)
REFRESH SECURITY TYPE(CONNAUTH)
